name: SITE-Docker

networks:
  proxy-net:
    name: proxy-net
    driver: bridge
  tunnel-net:
    name: tunnel-net
    driver: bridge

include:
  # --- Network ---
  - network/compose.yml

  # --- Homepage ---
  - home/compose.yml

  # --- Wiki ---
  - apps/bookstack/compose.yml

  # --- Other Apps ---
  - apps/speedtest/compose.yml
  #- path: ${APPS_PATH:?err}/twenty/compose.yml
  - apps/beszel/compose.yml
  - apps/autoxpose/compose.yml
  # --- Testing these apps out for now. ---
  # - apps/duplicati/compose.yml
  # - apps/nextcloud/compose.yml
  # - apps/paperless-ngx/compose.yml
  # - apps/vaultwarden/compose.yml
